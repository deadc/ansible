version: 2
jobs:
  build:
    docker:
      - image: tmaier/docker-compose:latest

    steps:
      - checkout
      - run:
          name: Build ansible image - Debian
          command: docker-compose build debian
      - run:
          name: Run ansible tests
          command: docker-compose run debian
