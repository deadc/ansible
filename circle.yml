machine:
  timezone:
    America/Sao_Paulo

  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose

debian:
  pre:
    - docker-compose build debian
  override:
    - docker-compose run debian
